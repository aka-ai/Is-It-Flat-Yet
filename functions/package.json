{
  "name": "functions",
  "description": "Cloud Functions for Firebase",
  "scripts": {
    "lint": "eslint .",
    "start": "firebase use staging && firebase functions:shell",
    "deploy:staging": "firebase use staging && firebase deploy --only functions && firebase use",
    "deploy:prod": "firebase use prod && firebase deploy --only functions && firebase use",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "8"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "csv-parse": "^4.8.8",
    "dayjs": "^1.8.22",
    "firebase-admin": "^8.6.0",
    "firebase-functions": "^3.3.0"
  },
  "devDependencies": {
    "@google-cloud/functions-framework": "^1.5.0",
    "eslint": "^5.12.0",
    "eslint-plugin-promise": "^4.0.1",
    "firebase-functions-test": "^0.1.6"
  },
  "private": true
}
